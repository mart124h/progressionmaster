import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'progressionmaster',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
